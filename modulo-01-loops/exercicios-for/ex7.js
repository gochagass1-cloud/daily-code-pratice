//Identificação de produto
let produtos = ['Notebook', 'Mouse', 'Teclado', 'Monitor', 'Headset']

for (let i = 0; i < produtos.length; i++) {
    console.log(`Produto ${produtos[i]} está na posição ${i}`);
}